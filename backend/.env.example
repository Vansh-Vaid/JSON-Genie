# Create a key at https://aistudio.google.com/app/apikey and keep it server-side.
GEMINI_API_KEY=your-gemini-api-key
# Optional: defaults to gemini-3.5-flash, which has free-tier availability subject to region and quota.
GEMINI_MODEL=gemini-3.5-flash
# For deployment, include your browser application's origin.
ALLOWED_ORIGINS=http://localhost:5173
